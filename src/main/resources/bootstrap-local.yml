#LOCAL Profile

#id-service runs on localhost:8081
#consul is on localhost:8500

server: 
  port: 8081
spring:
  cloud:
    consul:
      host: localhost
      port: 8500
eureka:
  shouldUseDns: false
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/v2