#DEV Profile

#id-service runs on dedicated dev machine, port 8080
#eureka is on discovery.dorffi.com, port 8080

server:
  port: 8080
eureka:
  shouldUseDns: true
  eurekaServer:
    context: eureka/v2
    domainName: discovery.dorffi.com
  region: us-west-1