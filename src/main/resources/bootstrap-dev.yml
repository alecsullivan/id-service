#DEV Profile

#id-service runs on dedicated dev machine, port 8080
#eureka is located via the discovery.dorffi.com domain.

server:
  port: 8080
eureka:
  shouldUseDns: true
  client:
    region: us-west-1
  eurekaServer:
    context: eureka/v2
    domainName: discovery.dorffi.com