#DEV Profile

#id-service runs on dedicated dev machine, port 8080
#eureka is on discovery.dorffi.com, port 8080

server:
  port: 8080
eureka:
  shouldUseDns: true
  client:
    serviceUrl:
      defaultZone: http://discovery.dorffi.com:8080/eureka/v2