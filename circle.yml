dependencies:
  post:
    - ./gradlew assemble

general:
  artifacts:
    - "build/libs"

deployment:
  development:
    branch: master
    codedeploy:
      id-service:
        application_root: /
        region: us-west-1
        revision_location:
          revision_type: S3
          s3_location:
            bucket: id-service
            key_pattern: id-service-{SHORT_COMMIT}-{BUILD_NUM}
        deployment_group: id-service
